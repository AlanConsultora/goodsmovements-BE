_schema-version: 3.3.0
ID: pushnotifications_goodsmovements
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: pushnotifications_goodsmovements-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: pushnotifications_goodsmovements-auth
      - name: pushnotifications_goodsmovements-connectivity
      - name: pushnotifications_goodsmovements-destination

resources:
  - name: pushnotifications_goodsmovements-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: pushnotifications_goodsmovements-${org}-${space}
        tenant-mode: dedicated
  - name: pushnotifications_goodsmovements-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
  - name: pushnotifications_goodsmovements-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
